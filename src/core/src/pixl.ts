import { DeviceController } from './device-controller';
import { IPixl } from './types/pixl-types';

export class Pixl extends DeviceController implements IPixl {
  // Backlight (on, off, toggle)
  // Contrast
  // turn screen off (Pixl.setLCDPower(true/false))
  // splash screen (set())
  // Terminal (println())
  // LED
  // Buttons
  // NFC
}
