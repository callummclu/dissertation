import { DeviceController } from './device-controller';
import { IBangle } from './types/bangle-types';

export class Bangle extends DeviceController implements IBangle {
  // Basic menu implementation, do not fully re implement this dont be silly.
  // Fake LEDS
  // Buzz
  // Sound
  // Buttons
  // Accelerometer
  // Gestures
  // GPS
  // Heart Rate
}
